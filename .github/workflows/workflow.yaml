name: Trigger branch placeholder

on:
  workflow_dispatch:

jobs:
  placeholder:
    runs-on: ubuntu-22.04
    steps:
      - run: echo 'This disposable branch only triggers the focused TXW81x finalization workflow.'
