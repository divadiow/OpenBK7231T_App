name: Build App (disabled on ECR6600 raw sleep test branch)

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Normal OpenBeken matrix intentionally disabled on this temporary test branch."
