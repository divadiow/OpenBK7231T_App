--- a/Boards/ecr6600/common/ld_xip.script.S
+++ b/Boards/ecr6600/common/ld_xip.script.S
@@ -24,6 +24,10 @@
   .text : 
     {
 	KEEP(*(.vector_init ));
+	/* psm_enter_sleep() calls these board clock routines while XIP flash is
+	 * unavailable. Keep the owning object in ILM without pulling the full
+	 * OpenBeken libboard archive into the constrained internal region. */
+	*libboard.a:chip_clk_ctrl.o(.text .text.*)
 	*libpsm.a:(.text .text.*)
 
 	TEXT_BEGIN_DRV = .;

--- a/Boards/ecr6600/common/ld_xip_yy.script.S
+++ b/Boards/ecr6600/common/ld_xip_yy.script.S
@@ -24,6 +24,10 @@
   .text : 
     {
 	KEEP(*(.vector_init ));
+	/* psm_enter_sleep() calls these board clock routines while XIP flash is
+	 * unavailable. Keep the owning object in ILM without pulling the full
+	 * OpenBeken libboard archive into the constrained internal region. */
+	*libboard.a:chip_clk_ctrl.o(.text .text.*)
 	*libpsm.a:(.text .text.*)
 
 	TEXT_BEGIN_DRV = .;
